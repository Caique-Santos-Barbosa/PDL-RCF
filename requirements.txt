# Dependências principais
flask>=2.0.0
flask-socketio>=5.0.0
python-socketio>=5.0.0
eventlet>=0.33.0

# Processamento de imagem e reconhecimento facial
opencv-python>=4.8.0
face-recognition>=1.3.0
numpy>=1.24.0
Pillow>=10.0.0

# Requisições HTTP
requests>=2.31.0

# Utilitários
werkzeug>=2.0.0
cmake>=3.27.0

# Para produção (Render) - dlib será compilado automaticamente
# dlib-binary>=19.22.0  # Removido - causando problemas no Render

# Dependências para produção (Render)
gunicorn>=20.0.0
gevent>=22.0.0
gevent-websocket>=0.10.1

# Dependências de desenvolvimento (opcional)
# pytest>=7.0.0
# black>=22.0.0
# flake8>=5.0.0
# mypy>=1.0.0

# Nota: O dlib será compilado automaticamente no Render
# Se houver problemas, o Render pode compilar o dlib automaticamente

